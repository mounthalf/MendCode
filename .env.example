# Use openai-compatible for generic chat-completions APIs.
# Minimax users may set MENDCODE_PROVIDER=minimax as an alias.
MENDCODE_PROVIDER=openai-compatible
MENDCODE_MODEL=
MENDCODE_BASE_URL=
MENDCODE_API_KEY=
MENDCODE_PROVIDER_TIMEOUT_SECONDS=60
